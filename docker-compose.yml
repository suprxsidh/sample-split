services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-this-to-a-random-secret-key}
      - FLASK_ENV=production
    restart: unless-stopped
